/* eslint-disable no-undef */

describe('Integration test suite template', () => {
  beforeAll(() => {

  });

  afterAll(() => {

  });

  beforeEach(() => {

  });

  afterEach(() => {

  });

  test('can calculate 2 plus 2', () => {
    expect(2 + 2).toBe(4);
  });
});

describe('Integration test suite template (2)', () => {
  beforeAll(() => {

  });

  afterAll(() => {

  });

  beforeEach(() => {

  });

  afterEach(() => {

  });

  test('can calculate 2 plus 2', () => {
    expect(2 + 2).toBe(4);
  });
});
