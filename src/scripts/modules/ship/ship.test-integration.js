

describe('Integration test suite template', () => {
  beforeAll(() => { });

  afterAll(() => { });

  beforeEach(() => { });

  afterEach(() => { });

  test.skip('can calculate 1 plus 1', () => {
    expect(1 + 1).toBe(2);
  });
});
