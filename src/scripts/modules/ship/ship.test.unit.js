/* eslint-disable no-undef */

describe('Unit test suite (1) template', () => {
  beforeAll(() => {

  });

  afterAll(() => {

  });

  beforeEach(() => {

  });

  afterEach(() => {

  });

  test('can calculate 2 plus 2', () => {
    expect(2 + 2).toBe(4);
  });
});
