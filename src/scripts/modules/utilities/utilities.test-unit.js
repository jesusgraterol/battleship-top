/* eslint-disable no-undef */

describe('Unit test suite (1) template', () => {
  beforeAll(() => {

  });

  afterAll(() => {

  });

  beforeEach(() => {

  });

  afterEach(() => {

  });

  test('can calculate 4 plus 4', () => {
    expect(4 + 4).toBe(8);
  });
});
