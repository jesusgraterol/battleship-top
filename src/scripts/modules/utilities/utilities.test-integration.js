




describe('Integration test suite template', () => {
  beforeAll(() => { });

  afterAll(() => { });

  beforeEach(() => { });

  afterEach(() => { });

  test.skip('can calculate 3 plus 3', () => {
    expect(3 + 3).toBe(6);
  });
});
