/* eslint-disable no-undef */

describe('Integration test suite template', () => {
  beforeAll(() => {

  });

  afterAll(() => {

  });

  beforeEach(() => {

  });

  afterEach(() => {

  });

  test('can calculate 3 plus 3', () => {
    expect(3 + 3).toBe(6);
  });
});
